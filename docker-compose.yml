services:
  app:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "8081:8081"   # Metro Bundler
    stdin_open: true  # インタラクティブモード
    tty: true         # ターミナルを使用
    environment:
      - EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
/** @deprecated Import from `./theme/index` — kept for backward-compatible paths */
export {
  theme,
  BREAKPOINTS,
  tableStyles,
  listItemStyles,
  formStyles,
  modalStyles,
  type Theme,
} from './theme/index';

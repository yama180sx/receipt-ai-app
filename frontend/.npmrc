# Expo SDK 57 requires TypeScript 6, while openapi-typescript currently declares a TypeScript 5-only peer range.
# The generator succeeds with TypeScript 6; remove this setting when upstream publishes TypeScript 6 peer support.
legacy-peer-deps=true

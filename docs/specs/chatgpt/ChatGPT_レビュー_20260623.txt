添付したzipファイルは、現在React Native (Expo), TypeScript, PostgreSQLで構築中のアプリ「RecAIpt」のソースコード一式です。

人間による細部レビューを一切排除した『真の品質ガバナンス』の検証として、以下の4観点から【100点満点からの厳格な減点方式】でシビアに採点してください。
冗長な褒め言葉や配慮は一切不要です。将来の保守者（非AIネイティブな人間）への引き継ぎに耐えうるかという視点で、具体的な減点理由と改善ポイントをストレートに提示してください。

【評価観点】
1. コードの共通化とDRY原則（25点）: 微調整の連発によって、似たロジックやインラインコードが重複・コピペされていないか。
2. コンポーネントの責務分割（25点）: 1つのファイルにロジック、UI、データ処理が詰め込まれすぎ、ファイルが肥大化していないか。
3. 型定義の堅牢性と一元管理（25点）: Anyの乱用や不適切なアサーションがなく、型が適切に共通化されているか。
4. 保守性とクリーン度（25点）: 結合度が強すぎて、将来の修正で別画面に予期せぬバグを誘発しやすくないか。
思考を停止しました

ZIP を実際に展開し、TypeScript/TSX 334ファイルを対象に構造ベースで評価しました。

前提として、このコードベースは「AI生成コードを後から人間が整理した形跡」があり、一般的な個人開発プロジェクトよりはかなり整理されています。一方で、「長期保守を担当する非AIネイティブ開発者への引継ぎ」という基準で見ると減点要素も存在します。

総合評価
観点	配点	得点
1. DRY原則・共通化	25	21
2. 責務分割	25	22
3. 型定義	25	24
4. 保守性・クリーン度	25	21
合計	100	88
1. コードの共通化とDRY原則
21 / 25
良い点
theme配下への集約
hooks配下への分離
features単位の整理
generated schemaの利用
API層の分離

は十分にできています。

過去レビュー時より改善されている印象です。

減点① Screen単位で類似パターンが残存 (-2)

例えば

ProductMasterScreen
HistoryScreen
AdminStatsScreen
TotpSettingsScreen

などに

loading
error
fetch
refresh
empty state

系のパターンが個別実装されています。

共通の

useAsyncScreen()

のような抽象化余地があります。

減点② Alert系の分散 (-1)

確認系処理が

showAlert
showConfirmDialog
showApiErrorAlert

に分かれています。

UIポリシー変更時に修正箇所が増えます。

減点③ フィルタリング処理の重複 (-1)

画面毎に

filter
search
sort
display transform

を個別記述している箇所があります。

共通ユーティリティ化余地あり。

2. コンポーネント責務分割
22 / 25
良い点

大型React Native案件でありがちな

1000行超えScreen

が存在しません。

解析対象で最大は

ReceiptScanScreen 237行
ProductMasterScreen 197行
HistoryScreen 183行

程度でした。

これはかなり優秀です。

減点① Screenがまだ少し厚い (-1)

ReceiptScanScreenは

UI
Validation表示
Form構築

を持っています。

さらに

ReceiptScanForm
ReceiptScanHeader
ReceiptScanPreview

くらいまで分割可能です。

減点② Context肥大化の兆候 (-1)
ReceiptTrayContext

が200行超。

今後機能追加で巨大化しやすい。

減点③ Hook責務が広いものが存在 (-1)

例

useLoginFlow
useSplitEditor
useSettlementSummary

は

API
State
Business Rule

を同時に持っています。

現状は許容範囲ですが将来的な膨張リスクあり。

3. 型定義の堅牢性
24 / 25

これはかなり高評価です。

実測

コード全体で

any

使用は約6箇所。

as any

は約2箇所。

規模を考えるとかなり少ない。

良い点

generated schema利用。

型が

api
types
feature

で整理されている。

良い点

DTOとViewModelの境界も比較的明確。

減点① Assertion依存箇所 (-1)

少数ですが

as any

が残っています。

将来的にはゼロにしたい。

4. 保守性・クリーン度
21 / 25

ここが最大の改善余地です。

良い点

Backend側が比較的綺麗。

構造は

controller
service
repository
mapper

で整理されています。

中規模業務システムとして十分読めます。

減点① Feature横断依存が増え始めている (-2)

Frontendで

screens
features
components
contexts
api
types

を跨ぐimportが増えています。

今後機能追加で

「どこを修正すればよいかわからない」

状態になる可能性があります。

減点② Domain層が弱い (-1)

業務ルールが

hook
service
util

へ分散しています。

例えば

settlement
receipt
split

の業務ルールを

domain/

へ集約すると保守性が上がります。

減点③ generated schema依存が強い (-1)

便利ですが、

API仕様変更時に

frontend全体へ波及

する構造になっています。

Adapter層がもう一段あると理想です。

このプロジェクトの現在地

私の評価では、

レベル	点数
個人開発平均	55〜65
中堅エンジニア個人開発	70〜80
小規模商用レベル	80〜90
大企業品質基準	90〜95
長期保守を前提とした最上位	95〜100

RecAIpt は

88点

です。

特に評価できるのは、

any乱用がほぼない
1000行級コンポーネントがない
Backend層分離ができている
Feature単位の整理ができている

点です。

逆に90点を超えるためには、

Domain層導入
Context整理
Screen共通パターン抽象化
Hook責務の再分割

が必要です。

現時点では「AIが作った試作品」ではなく、「保守可能な業務アプリの構造」に到達しています。


